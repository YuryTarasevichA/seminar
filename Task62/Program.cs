/* Задача 62. Напишите программу, которая заполнит спирально массив 4 на 4.
Например, на выходе получается вот такой массив:
01 02 03 04
12 13 14 05
11 16 15 06
10 09 08 07 */

int[,] resultArray = CreateArrayMatrix(4, 4, 0, 100);
Console.WriteLine("Исходная матрица:");
PrintMatrix(resultArray);

int[,] CreateArrayMatrix(int rows, int colum, int min, int max)
{
    int[,] array = new int[4, 4];
    int value = 1;
    int rowStart = 0;
    int rowEnd = array.GetLength(0) - 1;
    int colStart = 0;
    int colEnd = array.GetLength(1) - 1;

    while (rowStart <= rowEnd && colStart <= colEnd)
    {
        // Заполнить верхнюю строку слева направо
        for (int i = colStart; i <= colEnd; i++)
        {
            array[rowStart, i] = value++;
        }
        rowStart++;

        // Заполнить правую колонку сверху вниз
        for (int i = rowStart; i <= rowEnd; i++)
        {
            array[i, colEnd] = value++;
        }
        colEnd--;

        if (rowStart <= rowEnd)
        {
            // Заполнить нижний ряд справа налево
            for (int i = colEnd; i >= colStart; i--)
            {
                array[rowEnd, i] = value++;
            }
            rowEnd--;
        }

        if (colStart <= colEnd)
        {
            // Заполнить левую колонку снизу вверх
            for (int i = rowEnd; i >= rowStart; i--)
            {
                array[i, colStart] = value++;
            }
            colStart++;
        }
    }
    return array;
}
void PrintMatrix(int[,] array)
{
    // Распечатать полученный спиральный массив
    for (int i = 0; i < array.GetLength(0); i++)
    {
        for (int j = 0; j < array.GetLength(1); j++)
        {
            Console.Write("{0:D2} ", array[i, j]);
        }
        Console.WriteLine();
    }
}
/* 1. Создание двумерного массива resultArray размером 4x4 и заполнение его значениями, полученными из функции CreateArrayMatrix с параметрами: 4 строки, 4 столбца, минимальное значение 0 и максимальное значение 100.
2. Вывод на консоль строки "Исходная матрица:".
3. Вызов функции PrintMatrix с параметром resultArray для печати значений массива.
4. Определение функции CreateArrayMatrix с параметрами: количество строк rows, количество столбцов colum, минимальное значение min и максимальное значение max.
5. Создание двумерного массива array размером 4x4.
6. Инициализация переменной value равной 1 - это будет первое заполненное значение в массиве.
7. Инициализация переменных rowStart, rowEnd, colStart и colEnd для определения границ заполнения массива по строкам и столбцам.
8. Запуск цикла while для заполнения массива в спиральном порядке до тех пор, пока не будут заполнены все ячейки.
9. Внутри цикла while запускается цикл for для заполнения верхней строки слева направо значениями от value до последнего элемента в этой строке (colEnd).
10. После этого увеличивается rowStart на единицу - это означает, что верхняя строка уже была заполнена и можно перейти к следующей строке.
11. Запускается цикл for для заполнения правой колонки сверху вниз значениями от value до последнего элемента в этой колонке (rowEnd).
12. После этого уменьшается colEnd на единицу - это означает, что правая колонка уже была заполнена и можно перейти к следующей колонке.
13. Если rowStart меньше или равно rowEnd, то запускается цикл for для заполнения нижней строки справа налево значениями от value до последнего элемента в этой строке (colStart).
14. После этого уменьшается rowEnd на единицу - это означает, что нижняя строка уже была заполнена и можно перейти к предыдущей строке.
15. Если colStart меньше или равно colEnd, то запускается цикл for для заполнения левой колонки снизу вверх значениями от value до последнего элемента в этой колонке (rowStart).
16. После этого увеличивается colStart на единицу - это означает, что левая колонка уже была заполнена и можно перейти к следующей колонке.
17. В конце функция возвращает массив array со всеми заполненными значениями.
18. Определение функции PrintMatrix с параметром array для печати значений массива.
19. Запуск цикла for для печати значений массива по строкам и столбцам с
 */